# nightwatch-selectize

## Install

`npm install`
`bower install`

## Setup for testing

Edit `nightwatch.json` to add your Browserstack credentials.

Edit `tests/test` to set a publicly accessible url to the demo page (I use ngrok).
